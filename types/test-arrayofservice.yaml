services:
  - apiVersion: v1
    kind: Service
    metadata:
     name: io.typefox.theia-ide.che-service.mainservice
    spec:
     selector:
       app: theia
     ports:
     - protocol: TCP
       port: 80
       targetPort: 9376
     - protocol: TCP
       port: 81
       targetPort: 8082
