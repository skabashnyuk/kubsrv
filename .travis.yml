sudo: false
language: go
go:
  - 1.10.x
before_install:
  - go get -u github.com/golang/dep/cmd/dep
  - dep ensure
script:
  - go test
matrix:
  allow_failures:
    - go: master
  fast_finish: true
notifications:
  email: false